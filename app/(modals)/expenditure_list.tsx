import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ExpenditureList = () => {
  return (
    <View>
      <Text>ExpenditureList</Text>
    </View>
  );
};

export default ExpenditureList;

const styles = StyleSheet.create({});
